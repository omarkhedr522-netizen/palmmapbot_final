# PalmMapBot Robot Control Requirements
# For Raspberry Pi GPIO relay control

# GPIO libraries (at least one required)
gpiozero>=1.6.2
RPi.GPIO>=0.7.0; platform_system=="Linux" and platform_machine=="armv7l" or platform_machine=="aarch64"

# Core dependencies
opencv-python>=4.5.0
numpy>=1.20.0
ultralytics>=8.0.0

# PyTorch for binary classifier
torch>=1.9.0
torchvision>=0.10.0
Pillow>=8.0.0

# I2C sensors
smbus2>=0.13.0

# Dashboard
flask>=2.0.0